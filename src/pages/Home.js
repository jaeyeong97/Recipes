import SearchBar from "../components/SearchBar";
import Response from "../components/Response";

const Home = () => {
    return (
        <div className="home">
            <SearchBar />
            <Response />
        </div>
    );
};

export default Home;